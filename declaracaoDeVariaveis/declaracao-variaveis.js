const base = 10;
//usa-se o tipo const para variáveis que não irão mudar o seu valor
const altura = 5;

//como area terá seu valor alterado usa-se o tipo let
let area = base * altura;

console.log(area);